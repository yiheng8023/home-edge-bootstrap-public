# 公开发布就绪边界

[English](../RELEASE_READINESS.md) · [首页](../../README.zh-CN.md)

本文定义可从本源码树检查的证据，不是发布批准，也不代表真实路由器状态。

## 源码树检查

- PowerShell 5 解析与 POSIX `sh -n` 语法检查通过。
- 合成的离线 fixture 套件通过，且不连接路由器或网络。
- 兼容性政策可解析，采用 `home-edge-public-compatibility/v1`，并保持 `field_evidence` 为空。
- 兼容性政策保持适配器成熟度与目标支持彼此独立，并将 Merlin 实现记录为尚未取得
  “已验证”成熟阶段的已实现参考适配器。
- 机密扫描、许可证存在性、SPDX 2.3 结构、双语文档链接与公开收口结构检查通过。
- 本地验证的稳定结果键为 `local_verification_state=ready`。

## 离线发布物检查

离线恢复发布物还需单独验证校验和、完整的运行时软件包/来源数据、第三方许可证材料、归档
内容、宿主机平台执行证据，以及发布专用 SBOM 与清单。这些检查针对具体发布物，不能由源码树
就绪替代。

## 声明边界

源码树就绪不会认证路由器型号、固件、提供方、真实网络行为、安全结果、持续可用性或部署成功，也不提供支持服务等级。发布决策必须审查具体源码提交和具体离线发布物。
